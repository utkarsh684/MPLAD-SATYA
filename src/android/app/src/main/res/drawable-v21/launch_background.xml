<?xml version="1.0" encoding="utf-8"?>
<!--
  Native launch screen. Shown by Android between process start and the first
  Flutter frame. It deliberately mirrors SplashScreen in Dart — same navy
  ground, same centred mark — so the handover is invisible rather than a
  white flash followed by a dark screen.
-->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:drawable="@color/splash_background" />
    <item>
        <bitmap
            android:gravity="center"
            android:src="@drawable/splash_logo" />
    </item>
</layer-list>
